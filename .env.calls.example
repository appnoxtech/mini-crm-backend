# CRM Call Feature - Environment Variables Example
# Copy this file to .env and fill in your values

# ============================================
# Existing Configuration
# ============================================

# Database Configuration
DATABASE_PATH=data.db

# Server Configuration
PORT=4000
FRONTEND_URL=http://localhost:5173
API_BASE_URL=http://localhost:4000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
TOKEN_ENCRYPTION_KEY=your-token-encryption-key-change-in-production

# Google OAuth Configuration (Required for Gmail integration)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4000/api/emails/oauth/gmail/callback

# Microsoft OAuth Configuration (Optional for Outlook integration)
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret

# Email Tracking
TRACKING_BASE_URL=http://localhost:4000

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# RunPod Configuration (for AI features)
RUNPOD_API_URL=https://api.runpod.ai/v2/your-endpoint-id/run
RUNPOD_API_KEY=your-runpod-api-key

# ============================================
# NEW: Twilio Configuration for Call Feature
# ============================================

# Twilio Account Credentials
# Get these from https://console.twilio.com/
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Twilio API Key (Recommended for token generation)
# Create at: https://console.twilio.com/us1/account/keys-credentials/api-keys
TWILIO_API_KEY_SID=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_API_KEY_SECRET=your-api-key-secret

# TwiML Application SID
# Create a TwiML App at: https://console.twilio.com/us1/develop/voice/manage/twiml-apps
# Set Voice Request URL to: https://your-domain.com/api/webhooks/twilio/voice
TWILIO_TWIML_APP_SID=APxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Verified Phone Number for Outgoing Calls
# This must be a Twilio phone number you own
# Or a verified caller ID: https://console.twilio.com/us1/develop/voice/manage/verified-caller-ids
TWILIO_CALLER_ID=+1234567890

# Recording Settings
TWILIO_RECORDING_ENABLED=true
TWILIO_TRANSCRIPTION_ENABLED=false
TWILIO_RECORDING_CHANNELS=dual

# Webhook Base URL (your publicly accessible server URL)
# Use ngrok for local development: ngrok http 4000
TWILIO_WEBHOOK_BASE_URL=https://your-domain.com

# ============================================
# Production Environment Notes
# ============================================

# 1. For production, set up proper SSL certificates
# 2. Update FRONTEND_URL and API_BASE_URL to production domains
# 3. Use strong, unique values for JWT_SECRET and TOKEN_ENCRYPTION_KEY
# 4. Ensure webhook URLs are publicly accessible
# 5. Configure Twilio geographic permissions for international calling
